#!/usr/bin/make -f

PYVERS=$(shell pyversion -vs)

%:
	dh $@

build:$(PYVERS:%=build-python%)
	touch $@
build-python%:
	python$* setup.py build
	touch $@

install: build $(PYVERS:%=install-python%)
install-python%:
	python$* debian/setup.py install --root $(CURDIR)/debian/pytango
